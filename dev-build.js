/**
 * Development build script
 * Loads variables from .env file and runs the build script
 */

// Load environment variables from .env file
require('dotenv').config();

// Run the build script
require('./build-config'); 